[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning

markers =
    smoke: Critical path checks; keep fast for CI smoke jobs.
    regression: Broader coverage including negative paths and edge scenarios.
